require("modules/env")
require("modules/autostart")
require("modules/monitors")
require("modules/decoration")

hl.config({
    input = {
        kb_layout = "br",
        kb_variant = "abnt2",
    },
})

require("modules/keybinds")
